spring:
  application:
    name: youtube-summarizer

server:
  port: 8080

app:
  gemini:
    api-key: ${GEMINI_API_KEY:}
    model: gemini-2.5-flash
  transcript-service:
    url: ${TRANSCRIPT_SERVICE_URL:http://localhost:5001}

logging:
  level:
    com.example.youtubesummarizer: DEBUG
